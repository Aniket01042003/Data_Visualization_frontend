import React from 'react'
import ChartCard from '../ChartCard.jsx'

function StatisticsData({filesData}) {
  return (
    <div>
      <ChartCard filesData={filesData} />
    </div>
  )
}

export default StatisticsData
